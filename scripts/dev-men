#!/bin/sh

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_dir=$(dirname -- "$script_dir")
dev_home=${MENISCUS_DEV_HOME:-"$repo_dir/.meniscus-dev"}

MENISCUS_HOME="$dev_home" exec "$repo_dir/.venv/bin/men" "$@"
