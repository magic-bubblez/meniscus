# Copy this file to backend/.env and fill in your key.
# When GEMINI_API_KEY is unset, /query falls back to deterministic synthesis
# so the demo still works — but the agent story is much weaker without it.


